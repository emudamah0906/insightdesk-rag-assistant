# --- Core RAG + EDA run with ONLY the stdlib + these two (already common). ---
pandas>=2.0          # EDA (scripts/01_eda.py)
# numpy is pulled in by pandas

# --- Optional: enable each milestone when you reach it ---
torch>=2.0           # src/classifier_torch.py  (PyTorch intent classifier)
fastapi>=0.110       # src/api.py  (serving)
uvicorn>=0.29        # run the API
pydantic>=2.0        # request models
streamlit>=1.34       # src/app.py  (chat UI:  streamlit run src/app.py)
anthropic>=0.40      # real LLM generation (set ANTHROPIC_API_KEY); else offline mock
# boto3>=1.34         # uncomment to call AWS Bedrock/SageMaker
